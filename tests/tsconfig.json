{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "..",
    "types": ["vitest/globals"],
    "baseUrl": ".",
    "paths": {
      "@stepflow/*": ["../src/*"]
    }
  },
  "include": ["**/*.ts", "../src/**/*.ts", "../src/**/*.d.ts"],
  "exclude": ["../node_modules", "../dist"]
}
