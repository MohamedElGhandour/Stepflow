export function start() {
    const container = document.createElement("div")
container.innerHTML = '<div class="onboarding-overlay" /> <div  class="onboarding-highlight" />';
document.body.appendChild(container);
}