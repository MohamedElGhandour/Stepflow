export enum StatusEnum {
  active = "active",
  completed = "completed",
  error = "error",
  idle = "idle",
}

export enum PlacementEnum {
  top = "top",
  bottom = "bottom",
  left = "left",
  right = "right",
  auto = "auto",
}

export enum ProgressIndicatorTypesEnum {
  dots = "dots",
  counter = "counter",
  percentage = "percentage",
  custom = "custom",
}

export enum ProgressIndicatorPositionEnum {
  header = "header",
  footer = "footer",
  inline = "inline",
}
